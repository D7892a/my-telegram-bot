# ربط تكسي دجلة بقاعدة بيانات Firebase 🔥

الموقع الآن مربوط فعلياً بمشروع **`tixs-73719`** عبر:

| الخدمة | الاستخدام |
|---|---|
| **Firebase Authentication** | كلمات مرور حقيقية مشفّرة عند Google (زبون / سائق / أدمن) |
| **Cloud Firestore** | كل البيانات: الحسابات، الرحلات، المحفظة، الكوبونات، التذاكر، الإعدادات، معلومات الدعم |
| **Firestore Rules** | حماية حقيقية: لا أحد يدخل لوحة الإدارة أو يعدّل الأسعار إلا الأدمن |

---

## خطوات التشغيل (مرة واحدة فقط)

### 1) تفعيل تسجيل الدخول
Firebase Console → **Build → Authentication → Get started → Sign-in method** → فعّل **Email/Password** → Save.

### 2) إنشاء قاعدة البيانات
**Build → Firestore Database → Create database** → اختر Region قريب (`europe-west1` مثلاً) → Create.

### 3) لصق قواعد الحماية
**Firestore Database → Rules** → امسح الموجود والصق كامل محتوى ملف [`firestore.rules`](firestore.rules) → **Publish**.

> أو من الطرفية إذا عندك Firebase CLI:
> ```bash
> npm i -g firebase-tools
> firebase login
> firebase deploy --only firestore:rules
> ```

### 4) تهيئة البيانات وإنشاء الأدمن
افتح صفحة **`setup.html`** من الموقع (مثلاً `https://موقعك/setup.html`) ثم:

1. تأكد أن "حالة الاتصال" تظهر ✅ متصل بـ Firestore.
2. اكتب **بريد الأدمن** و**كلمة مرور قوية (8 أحرف فأكثر)** ← اضغط **إنشاء أدمن**.
   - كلمة المرور هذه تُخزَّن مشفّرة داخل Firebase ولا تظهر في أي ملف من ملفات الموقع.
   - إنشاء أول أدمن مسموح **مرة واحدة فقط**، وبعدها يُقفل تلقائياً بمستند `config/bootstrap`.
3. اضغط **تعبئة قاعدة البيانات** لرفع الإعدادات والتسعير ومعلومات الدعم والسائقين والرحلات.
4. (اختياري) اترك خيار الحسابات التجريبية مفعّلاً لإنشاء `ahmed@dijla.iq` و `karim@dijla.iq` بكلمة مرور `123456`.

### 5) بعد الانتهاء
احذف ملف `setup.html` من الاستضافة (أو انقله) — لم تعد بحاجته، وقفل `config/bootstrap` يمنع إنشاء أدمن جديد على أي حال.

---

## ما الذي صار حقيقياً الآن؟

- 🔐 **دخول الإدارة**: لا توجد كلمة سر مكتوبة في الكود إطلاقاً. حساب `admin / admin` القديم **حُذف نهائياً**، وزر الدخول التجريبي للأدمن أُزيل من الواجهة.
- 🔑 **تغيير كلمة مرور الأدمن** من داخل اللوحة: `إعدادات النظام → حساب الإدارة` (يطلب كلمة المرور الحالية للتأكيد).
- 📩 **نسيت كلمة المرور؟** ترسل رابط استعادة حقيقي على بريد المستخدم.
- ☎️ **معلومات الدعم**: `إعدادات النظام → معلومات الدعم والتواصل` — رقم الاتصال، واتساب، الإيميل، العنوان، ساعات العمل، وروابط السوشيال. ما يكتبه الأدمن يُحفظ في `config/support` ويظهر **فوراً** في صفحة "اتصل بنا" وتذييل الموقع لكل الزوار.
- 🎫 **تذاكر الدعم**: أي رسالة من نموذج "اتصل بنا" تُحفظ في Firestore وتظهر للأدمن مع إمكانية إغلاقها.
- 🚕 **تزامن لحظي**: طلب رحلة أو اعتماد سائق أو تعديل سعر ينعكس على كل الأجهزة بدون تحديث الصفحة (`onSnapshot`).
- 📴 **وضع عدم الاتصال**: إذا انقطع الإنترنت يستمر الموقع بالعمل من النسخة المحلية، لكن **دخول الإدارة يبقى ممنوعاً** حتى يعود الاتصال.

---

## بنية البيانات في Firestore

```
accounts/{uid}          → { uid, role: customer|driver|admin, refId, email }
admins/{uid}            → { id, name, email, role }
customers/{id}          → بيانات الزبون + uid (بدون أي كلمة مرور)
drivers/{id}            → بيانات السائق + status: pending|approved|blocked
driverDocs/{id_kind}    → صور المستمسكات (حتى ~900KB للملف)
rides/{id}              → الرحلات
transactions/{id}       → حركات المحفظة
notifications/{id}      → الإشعارات
pendingRequests/{id}    → طلبات الرحلات الحيّة
tickets/{id}            → تذاكر الدعم
withdrawals/{id}        → طلبات سحب أرباح السائقين
promoCodes/{code}       → الكوبونات
config/settings         → إعدادات النظام
config/pricing          → التسعير والعمولة
config/support          → رقم وإيميل الدعم (يحرّرها الأدمن)
config/stats            → الإحصائيات
config/bootstrap        → قفل إنشاء أول أدمن
```

---

## ملاحظات أمنية

- مفاتيح `firebase-config.js` **عامة بطبيعتها** ويجب أن تصل للمتصفح؛ الحماية الحقيقية من `firestore.rules`. لا تضع أبداً ملف *Service Account JSON* داخل المشروع.
- إذا قيّدت مفتاح الـ API بـ HTTP referrers من Google Cloud Console، أضف نطاق موقعك.
- المستمسكات مخزّنة حالياً داخل Firestore (حد المستند ~1MB). لو أردت ملفات أكبر ننقلها إلى **Firebase Storage** (يحتاج تفعيل خطة Blaze).

---

## اختبار محلي

```bash
node tests/firebase-integration.test.js
```

يشغّل ملفات الموقع الحقيقية مقابل Firebase وهمي في الذاكرة ويتحقق من: رفض الأدمن الوهمي، الدخول بكلمة سر حقيقية، حفظ معلومات الدعم، تسجيل زبون جديد بدون تخزين كلمة مروره، حفظ الرحلات والتذاكر، واعتماد السائق.
