# 🔐 بيانات دخول الإدارة — تكسي دجلة (الناصرية)

> حساب الإدارة **جاهز ويُنشأ تلقائياً** أول مرة يُفتح فيها الموقع.
> لا تحتاج فتح `setup.html` ولا أي خطوة يدوية.

## بيانات الدخول

| الحقل | القيمة |
|---|---|
| **البريد الإلكتروني** | `admin@dijla.iq` |
| **كلمة المرور** | `Dijla@IE5NZTbpYo` |
| **الدور** | مدير عام (super_admin) |

**طريقة الدخول:** افتح الموقع → تسجيل الدخول → اختر «إدارة» → أدخل البريد وكلمة المرور أعلاه.

## كيف يعمل؟

- البيانات محفوظة في الملف: [`js/admin-config.js`](js/admin-config.js)
- عند أول تشغيل للموقع، ينشئ السكربت `js/admin-bootstrap.js` الحساب تلقائياً في
  Firebase (Auth + Firestore) ويقفل التهيئة حتى لا تتكرر.
- إذا كانت قاعدة البيانات فارغة، تُهيّأ الإعدادات الأساسية تلقائياً أيضاً.

## كيف أغيّر البيانات؟ ✏️

### قبل أول تشغيل للموقع
افتح `js/admin-config.js` وغيّر البريد أو كلمة المرور مباشرة — القيم الجديدة
هي التي ستُستخدم عند إنشاء الحساب.

### بعد إنشاء الحساب (الموقع فُتح ولو مرة واحدة)
الحساب أصبح مخزّناً في Firebase، لذلك غيّر كلمة المرور بإحدى الطرق:

1. **من داخل الموقع (الأسهل):** سجّل دخولك كإدارة → تبويب **الإعدادات** →
   بطاقة **حسابي** → أدخل كلمة المرور الحالية والجديدة → تغيير كلمة المرور.
2. **نسيت كلمة المرور؟** من شاشة الدخول اضغط «نسيت كلمة المرور» وسيصلك رابط
   إعادة التعيين على البريد.
3. **من Firebase Console:** Authentication → Users → `admin@dijla.iq` → Reset password.

## ⚠️ تنبيه أمني مهم

ملف `js/admin-config.js` يُحمَّل مع الموقع مثل أي ملف JavaScript، أي أنه
**مقروء لأي زائر**. لذلك بعد أول دخول ناجح **غيّر كلمة المرور من لوحة الإدارة**
— بعدها كلمة المرور الحقيقية تكون في Firebase فقط ولا يهم ما هو مكتوب في الملف.
